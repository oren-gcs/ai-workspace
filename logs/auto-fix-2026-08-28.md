# Auto-fix log — 2026-08-28

## Priority: Git Windows hang

### Done
1. Removed `index.lock` at all canonical winner `.git` paths — none found.
2. Killed 5 zombie `git.exe` (idle >37 min); stopped stuck recursive lock scan (PID 57260).
3. Ran `install-auto-push-hooks.ps1` — hooks on ai-workspace + doc-power-local-k8s.
4. Verified `spawn-auto-push.ps1` requires `-OnlyPath` (hooks pass it); no path bug.
5. Git config: longpaths, fsmonitor, submodule summary off (local + global ignore dirty submodules).
6. User env `GIT_TERMINAL_PROMPT=0`.
7. ai-workspace `.git/info/exclude` — `_archive/`, `node_modules/`, etc.
8. `sync-gh-auth.ps1` + `git-background-worker.ps1` + `auto-push.ps1` — all OK, nothing to push.
9. Timing: status **0.58s**, fetch **3.95s** (skipped empty commit — dirty tree).
10. Workspace `ai-workspace.code-workspace`: autorefresh off, maxConcurrentProcesses 2, smart commit on.

### Other backlog
- doc-power docker compose: stacks running; started prometheus + cadvisor where needed.
- Removed junk: `scripts/_gen_sync.py`, `scripts/sync-gh-auth-gcm.ps1`.
- F:\ root credentials quarantine: no credential files at F:\ root (verified).

### Skipped
- Empty commit test (modified ACTION-LOG, submodule pointer, untracked bootcamp dir).
- Browser / claude.ai — no action.

## Git timing after fix
| Command | Seconds |
|---|---|
| `git status --porcelain` (ai-workspace) | 0.58 |
| `git fetch --dry-run` | 3.95 |

## Issue resolved?
**Partial** — CLI responsive; IDE may need window reload to pick up workspace git settings.

## Manual follow-up
1. Reload Cursor window after opening `ai-workspace.code-workspace`.
2. If Source Control still hangs, temporarily disable Git extension autorefresh in user settings too.
3. Commit or ignore `projects/cka-ai-bootcamp/local/` when ready.
